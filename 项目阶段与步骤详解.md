# 项目阶段与步骤详解

> 状态标记：〔待开始〕〔进行中〕〔已完成〕〔已阻塞：原因〕
> 每完成一步更新对应条目并输出一行进度汇报（✅ 完成 [阶段] 步骤 [N]）。

## 阶段 P1a：统计指纹 〔进行中〕

加载原始分钟级数据（dtype 优化、只读需要的列），把 `?` 转 NaN 后只输出统计指纹（shape/dtype/缺失率/分位数/gap 长度分布/top 类别频次），不输出任何原始数据行。目的：在动手清洗前先摸清缺失的规模、形态与分布，为插补规则和分段策略提供量化依据；防止不看数据就拍脑袋定清洗规则。

### 步骤 1：环境检查与文档骨架
**步骤目的**：确认 Python/pandas 版本可用并留痕（后续复现与「环境版本留痕」验收项闭环）；一次性生成 P1a–P5 全部文档条目，让每步成果有落点。
**产出成果**：环境版本记录；本文件全阶段骨架。预期：Python≥3.10、pandas 可用 ｜ 实际：Python 3.11.9 / pandas 3.0.5 / numpy 2.3.2 ✅

### 步骤 2：dtype 优化读入原始数据
**步骤目的**：207 万行分钟级文件必须控制内存与读入时间，同时解析 Date/Time 为时间索引，为 gap 检测做准备。防坑：字符串列全量驻留内存既慢又易触发后续隐式类型问题。
**产出成果**：内存占用对比、行数、时间范围。预期：行数约 207 万、时间覆盖 2006–2010 ｜ 实际：行数 2,075,259（与理论分钟跨度完全相等，零整行缺席）、范围 2006-12-16 17:24 ~ 2010-11-26 21:02、时间严格递增无重复、float32 读入内存 329.9 MB ✅
🎤 面试 30 秒版：两分钟级数据 200 多万行，我第一步不是画图而是做 dtype 优化读入——数值列直接 float32、时间列解析成索引，先把内存和类型风险控制住，再谈分析。

### 步骤 3：统计指纹计算
**步骤目的**：量化缺失的规模与形态（单点 `?` 还是成段空窗、最长 gap 多长、>24h 空窗占比是否超 5% 红线），这是插补规则和分段策略的直接输入。防坑：不摸底就清洗，会把数周空窗中间插出一整条假数据。
**产出成果**：每列缺失率、数值分位数、gap 长度分布（整行缺席 + NaN 连续段两套口径）、>24h 空窗占比、top 频次。预期：>24h 空窗占比 <5% ｜ 实际：7 列缺失完全对齐（均 25,979 行/1.252%，整行级 `?`）；无整行缺席；目标列 NaN 连续段共 71 段（62 段 <1h、2 段 1–2h、1 段 2–24h、6 段 >24h，最长 120.4h）；>24h 空窗占总缺失时间 94.87%、占总跨度 1.19%（口径见汇报） ｜ 指纹文件 outputs/p1a_fingerprint.txt ✅
🎤 面试 30 秒版：缺失值我从不默认 interpolate。先打指纹：缺失率、分位数、gap 长度分布一起看，确认大空窗占比低于 5% 红线后才敢定分段插补规则——因为长 gap 强行插值等于造数据。

### 步骤 4：指纹汇总报告并等待确认
**步骤目的**：把指纹结论汇报给用户确认，拿到明确许可后才进入清洗（硬约束 1 的门禁）。防坑：跳过确认等于绕过数据层的最高门禁。
**产出成果**：指纹报告（仅统计量）。预期：用户确认后进入 P1b ｜ 实际：待填

---

## 阶段 P1b：清洗校验 〔已完成〕

按确认的插补规则做确定性清洗（<2h interpolate 且显式 `limit_direction='forward'`；2–24h ffill + 插补标记列；>24h 不插补作分段边界，窗口只在分段内构造），重采样到小时级（kW=mean、kWh=sum 两列分名），存 parquet；独立断言脚本校验行数守恒、时间非递减、关键列无 null、**kWh 能量守恒（容差 <0.5%，超出定位并报用户）**、lag/rolling 特征小样例对照。目的：让清洗每一步可复核、可复现。防坑：清洗出错会污染下游所有模型，所以校验脚本与清洗代码分离。

### 步骤 1：确定性插补与插补标记列
**步骤目的**：按指纹确认后的规则插补，全程确定性、无随机成分，保证可复现。
**产出成果**：插补后分钟级数据 + 标记列。预期：插补行占比与指纹统计吻合 ｜ 实际：interpolate 441 分钟行（64 段 <2h）、ffill 891 分钟行（1 段 14.85h）、>24h 六段零插补；flag 0=原始/1=interpolate/2=ffill/NaN=边界段 ✅
🎤 面试 30 秒版：插补我按 gap 长度分级且全程确定性：2 小时内线性插值、2 到 24 小时前向填充并打标记、超过 24 小时绝不插补直接当分段边界——因为给数周空窗造数据比缺失本身更有害。

### 步骤 2：分段边界与小时级重采样
**步骤目的**：>24h 空窗保留为分段边界；重采样规则写死 kW=mean、kWh=sum 另列，命名区分。防坑：聚合口径错（kW 取 sum）会让能量不守恒、下游全错。
**产出成果**：小时级 parquet（负荷列 + 能耗列 + 分段标记）。预期：能量守恒偏差 <0.5% ｜ 实际：小时有效 34,169h / 无效 420h；能量守恒偏差 0.033%（远低于 0.5% 红线）；分段统计表加列 n_windows（=段长−191，不足 192h 记 0），总建模窗口 32,832 ✅

### 步骤 3：独立不变量断言脚本
**步骤目的**：用与清洗代码独立的脚本校验不变量（硬约束 17），防「自己改自己查」的盲区。
**产出成果**：断言脚本全部通过的记录。预期：全部 PASS ｜ 实际：scripts/p1b_assert.py 15 项全部 PASS（行数守恒/时间非递减/段分类 64-1-6/长 gap 零插补/插补值域/ffill 常数/flag 语义/小时有效性/kW 与 kWh 口径/能量守恒 0.033%/lag-roll 小样例对照/分段窗口口径/总窗口≥3 万） ✅

---

## 阶段 P2：EDA 〔已完成〕

四张必含图：日内双峰曲线、工作日/周末对比、按年日均负荷曲线（概念漂移检查，**标注「为 RevIN 必要性提供数据证据」**）、gap 分布与分段边界图；功率因数常识校验（0.9~1）。目的：用图证回答「这个数据长什么样、漂移存不存在、RevIN 有没有必要」，为建模决策提供数据证据。

### 步骤 1：日内/周内模式
**步骤目的**：确认早晚双峰、工作日/周末差异的存在性与强度——这是季节朴素基线和 hour-of-day 特征的依据。
**产出成果**：日内双峰曲线、工作日/周末对比图。预期：双峰明显 ｜ 实际：双峰明确（早峰 ~7–8h、晚峰 ~20–21h）；工作日早峰（1.7kW）远高于周末（1.2kW），周末午间平台更平更持久——季节朴素与 hour-of-day/DOW 特征依据充分 ✅
🎤 面试 30 秒版：日内曲线是典型家庭负荷双峰——早峰是起床洗漱做早餐，晚峰是做饭热水加照明娱乐；工作日早峰比周末高四成、周末白天更平，说明负荷主要由作息驱动而不是天气，这让小时和星期几特征变得非常有价值。

### 步骤 2：年度漂移与 gap 分布
**步骤目的**：按年日均负荷曲线检查概念漂移（RevIN 的数据证据）；gap 分布与分段边界图把 P1a 的统计结论可视化。
**产出成果**：按年日均负荷曲线、gap 分布图。预期：存在可见漂移 ｜ 实际：年均负荷 2006=1.90（仅 12 月，冬季+入住初期偏高）→ 2007–2010 ≈1.06–1.12；年际基线与季节振幅均漂移（2008 夏谷 ~0.35kW、2006 冬 ~1.9kW），RevIN 窗口级归一化的必要性有数据证据；gap 图基于原始 `?` 掩码重画（防「清洗后数 gap」陷阱）✅

### 步骤 3：领域常识校验
**步骤目的**：功率因数 = Global_active_power / sqrt(GAP²+GRP²) 应在 0.9~1；反常识先查数据处理。
**产出成果**：功率因数分布统计。预期：0.9~1 ｜ 实际：中位数 0.993、P99=1.000；PF<0.9 集中于 0.1–1.0kW 中等负荷段（冰箱/水泵类感应电机），>1kW 负荷 PF>0.998、按年无异常——物理解释成立，非数据错误；Sub_metering 日均 1.6+1.9+9.3=12.8 kWh/day 占 GAP 26.2 kWh/day 的 48.8%，与 UCI 已知口径一致 ✅

---

## 阶段 P3：基线组 〔进行中：门禁已通过，待用户确认跑全量〕

时间顺序切 train/val/test（切分点写 README，不 shuffle）；全局 scaler 仅在训练段 fit 且**只用于基线模型**；5 基线齐全：persistence、季节朴素、滑动平均、DLinear、XGBoost（显式 hour-of-day、day-of-week 特征）；3-fold rolling origin CV 仅在 test 切分点之前滚动；核心指标双路径核对（Pandas vs SQL）。**采样门禁：先在同一份代码、仅日期参数不同的约 5 万行连续子集（完整覆盖至少一个已知缺失段）跑通，全部通过后停下等用户确认再做全量。** 开始前先提交复用白名单清单（复用旧项目哪几段、各自改哪里）等用户确认。

### 步骤 0：复用白名单清单确认
**步骤目的**：动手前明确旧项目哪些代码可复用、各自改哪里（硬约束·复用规约）。
**产出成果**：清单经用户确认。预期：确认后进入编码 ｜ 实际：白名单 5 项+黑名单替换表已确认（2026-09-10）；指标口径补充：主口径=扁平化整体 MAE，horizon 均值并报，README 注明 ✅

### 步骤 1：切分与特征工程（含泄漏审查）
**步骤目的**：时间顺序切分；XGBoost 特征显式 hour-of-day/DOW；逐条审查 shift/rolling 满足「信息可用时间 ≤ 预测时点」。防坑：旧项目当日聚合泄漏教训。
**产出成果**：切分点记录、特征清单与泄漏审查记录。预期：无一条特征越界 ｜ 实际：特征 15 个（hour/dow/is_weekend + lag{1,2,3,6,12,24,48,72,96,120,144,168}h + roll_mean/std_24h + roll_mean_168h），全部 ≤ t，逐条审查通过；切分按 origin 时间 70/15/15（不 shuffle）；门禁与全量同一脚本仅日期参数不同 ✅
🎤 面试 30 秒版：特征是 168 小时窗口内的滞后和滚动统计加日历特征，每一条都满足只用预测时点之前的信息——我逐条审过 shift 的方向和 rolling 的边界，因为旧项目就栽在当日聚合特征上，把全天的信息泄给了预测。

### 步骤 2：5 基线训练与 rolling origin CV
**步骤目的**：建齐基线组，CV 只在 test 切分点前滚动（硬约束 12）。
**产出成果**：基线结果表、CV 日志。预期：5 基线全部出数 ｜ 实际：门禁 5 基线出数；全量 32,832 origins（train 22,978 / val 4,929 / test 4,925，切分点 val≥2009-08-28、test≥2010-04-10），pooled MAE：persistence 0.8050 / seasonal_naive 0.5859 / ma7 0.4674(R²0.3455) / xgboost 0.4223(R²0.3330)，双路径全一致；rolling origin CV（修正后全部验证窗口 < test 切分点）：fold MAE 0.5941/0.5554/0.3867，均值 0.5121±0.0901（XGBoost 配置稳定性可接受）✅

### 步骤 3：双路径指标核对
**步骤目的**：核心指标用 Pandas 与 SQL 两条独立路径计算并一致（硬约束 18）。
**产出成果**：双路径核对记录。预期：完全一致 ｜ 实际：门禁 5 模型 × (MAE/RMSE/sMAPE/R²/hMAE) 全部 rel diff < 1e-6（SQLite SQL 路径 vs pandas）✅

### 步骤 4：采样门禁跑通并等待确认
**步骤目的**：约 5 万行连续子集先全流程跑通（硬约束 19），通过后停下等用户确认再全量。
**产出成果**：门禁验证报告。预期：用户确认 ｜ 实际：2009-06-01~07-06（35 天=50,400 分钟行，完整覆盖 06-13 的 52.2h 缺失段），403 origins，5 基线+双路径全部通过；待用户确认 ✅

---

## 阶段 P4：PatchTST 〔待开始〕

用现成时序深度学习库（neuralforecast/tsai）实现 PatchTST，**逐项对齐论文 §3.1（硬约束 13a）：patch_len=16、stride=8、序列末端补 S 个末值、RevIN 开启；库默认值冲突时停下报告，不得静默采用**；**训练损失 = MSE（13b），MAE 仅评估**；输入归一化走 RevIN，不经过全局 scaler（与基线的全局 scaler 分层、互不冲突，README 写明）；val + rolling origin CV 调参，范围与选择留痕（training_log.csv，对话只 tail）；时间盒 2 天 CPU，超时冻结最优进 P5。**OPT-1（patch 消融）全程冻结，P4 结束时汇报状态等用户决策，不自行解锁。**

### 步骤 1：库选型与环境核对
**步骤目的**：确认 neuralforecast/tsai 可用性与 PatchTST 参数默认值，逐项与论文 §3.1 对照；冲突即停下报告（13a）。
**产出成果**：默认值对照表。预期：16/8/RevIN 可配置 ｜ 实际：neuralforecast 3.2.2 PatchTST 默认 patch_len=16/stride=8/revin=True/n_heads=16/encoder_layers=3/dropout=0.2，与论文 §3.1 一致无冲突；d_model/d_ff 在该实现不可调（README 对照表注明）；默认 loss=MAE 已按 13b 显式改 MSE；batch_size 默认 32→256（用户上限）；torch 2.6.0+cu124+RTX 4060 验证通过；CPU 基准 200 步已录 training_log.csv ✅
🎤 面试 30 秒版：我没有手撸 PatchTST，而是用 neuralforecast 并对照论文逐项核验默认配置——patch 16、stride 8、RevIN、3 层 encoder 全对上了；唯一要动的是训练损失，库默认 MAE，论文训 MSE，我显式改回 MSE 并写进留痕。

### 步骤 2：PatchTST 训练（MSE 损失）
**步骤目的**：按论文配置训练，直接 24 输出；训练日志写 training_log.csv。
**产出成果**：训练日志、val 指标。预期：收敛正常 ｜ 实际：滚动 val 三模型（共同 origins 4,593，pooled MAE）：XGBoost 0.4727 ｜ PatchTST lr=5e-4 0.5788 ｜ lr=1e-4 0.6011 ｜ DLinear 0.6087；lr=5e-4 为最优 DL 配置；DL 落后 XGB ~0.1，与「大数据集才显著」预期一致。分块 bug 已修（见 decisions.md）。test 冻结待泄漏审查 ✅

### 步骤 3：调参与对比
**步骤目的**：在 val + rolling CV 上调参，与 P3 基线同口径对比；时间盒内操作，超时冻结。
**产出成果**：调参记录、对比表初版。预期：时间盒内收敛到最优 ｜ 实际：val 只动 lr 一档（1e-4→5e-4，ΔMAE -0.022），test 三模型滚动完成（断电一次，来电续跑无损）：共同 origins 4,589 pooled MAE：XGB 0.4190 ｜ ma7 0.4341 ｜ PatchTST 0.4894 ｜ DLinear 0.5011 ｜ seasonal 0.5105 ｜ persistence 0.6797；双路径核对全 OK；val→test 排序一致；horizon 曲线与耗时/显存数据已存 outputs/p5/（horizon_mae_test.csv、training_log.csv）✅

### 步骤 4：OPT-1 决策点汇报
**步骤目的**：P4 结束时汇报消融可行性与时间盒余量，等用户指令；保持冻结。
**产出成果**：决策点汇报。预期：用户决策 ｜ 实际：P4 GPU 用时约 4h/6h 预算（余 ~2h），rolling 结果稳定（两期排序一致、双路径核对通过）——是否解锁 OPT-1 随 8 项汇报提交用户决策，保持冻结 ✅

---

## 阶段 P5：收尾 〔待开始〕

粒度桥接（24h 预测聚日均 vs 旧项目日级）+ 新旧对比表（模型/粒度/horizon/MAE/R²/核心特征 Top3/一句话结论）；**horizon vs MAE 双曲线**（旧项目递归 0.3943→0.5761 vs PatchTST 直接多输出）；**「论文声称 vs 我的数据」对照表（≥3 条核心声称 + 自监督预训练不复现原因）**；误差归因（夜间/周末/漂移期）；泄漏排查 README 专节；README 全稿（PatchTST 配置逐项标论文出处、scaler/RevIN 分层说明、MAPE 口径差异）；环境版本留痕。

### 步骤 1：粒度桥接与新对比表
**步骤目的**：同口径对比是最高优先级硬约束；对比表缺一行即项目失败。
**产出成果**：完整对比表。预期：无缺行 ｜ 实际：B1 粒度桥接（n=191 统一，0.1504/0.1712/0.1866 vs 旧 0.1877）+ B2 对比表 11 行（用户修正朴素基线方向、importance 标注）✅

### 步骤 2：horizon-MAE 曲线与论文对照表
**步骤目的**：闭环「直接多输出消除递归误差累积」的验证；把论文阅读变成可核查证据。
**产出成果**：双曲线图、论文声称对照表。预期：直接多输出曲线明显更平 ｜ 实际：B3 双曲线（+21% vs 递归 +46%）+ B4 三维误差归因（Q4 短史段主因、夜间弱信号、周末、R² 分母效应 64%）+ B5 论文声称 7 条（C4 消融入表、C6 RevIN 边界）+ OPT-1 消融关闭（改善 0.0069kW/1.2%，定稿措辞）✅

### 步骤 3：误差归因与泄漏排查小节
**步骤目的**：误差最大时段单独归因；泄漏排查写成 README 专门小节（验收 4/6）。
**产出成果**：归因结论、泄漏排查小节。预期：完成 ｜ 实际：README §5 泄漏专节 6 案例 + §8 归因 ✅

### 步骤 4：README 全稿与仓库收尾
**步骤目的**：数字可复现、口径写明、配置标论文出处；交付可推送仓库。
**产出成果**：README.md 定稿、training_log.csv、decisions.md 完结。预期：完成 ｜ 实际：README 14 节全稿完成，待用户验收 ✅
