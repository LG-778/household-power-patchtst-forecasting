# decisions.md

一行一条：决策 + 理由。供跨会话续接。

- 2026-09-10（开工前确认）：interpolate 显式指定 `limit_direction='forward'`，不依赖 pandas 默认值。理由：默认值行为随版本可能变化，显式声明保证确定性与可复现（用户修订①）。
- 2026-09-10（开工前确认）：kWh 能量守恒断言容差 <0.5% 视为通过，超出需定位差异并报用户。理由：float32 精度与聚合舍入存在固有误差，零容差不现实；超差则必有口径错误，必须定位（用户修订②）。
- 2026-09-10（开工前确认）：复用白名单清单不在开工时提交，延至 P3 开始前（步骤 0）。理由：P1a/P1b/P2 不涉及旧项目代码。
- 2026-09-10（P1b）：插补改为逐段 interpolate：pandas 全序列 `interpolate(limit=119, limit_direction='forward', limit_area='inside')` 的 limit 语义是「每个有效值之后最多连续填 N 个」，长 gap 的前 119 分钟会被误填（实测 6×119 分钟，其中每小时段尾部 1 个假小时混入分段）。改为只在短段内部逐段线性插值，并加硬断言「长 gap 零插补」。
- 2026-09-10（P1b）：小时有效性规则写死「60 分钟全部有值（含插补值）才算有效小时」，跨界小时置 NaN 自动成分段边界。理由：避免部分小时（如 gap 旁只有 20/60 有效分钟）混入建模。
- 2026-09-10（P1b）：impute_flag 语义定稿：0=原始、1=interpolate(<2h)、2=ffill(2–24h)、NaN=>24h 边界段；小时级取段内最大值。能量守恒实测偏差 0.033%，远低于 0.5% 容差。
- 2026-09-10（P2）：EDA 关键结论——早晚双峰明确；工作日早峰≈1.7kW 远高于周末≈1.2kW；年际基线漂移明显（2006 冬 1.9kW → 2008 夏谷 0.35kW），RevIN 必要性成立；PF 中位 0.993，低 PF 集中在 0.1–1kW 中等负荷（感应电机），非数据错误；Sub_metering 覆盖 GAP 的 48.8%，确认其余为未分项负荷，EDA-only 决策不变。
- 2026-09-10（P3 步骤 0）：复用白名单 5 项确认（指标计算/对比表结构/读入解析/绘图模式/README 骨架），黑名单全部替换；pf_daily/pf_hourly 特征不搬（单变量任务+旧项目泄漏教训）。
- 2026-09-10（P3 指标口径，用户补充）：24 个 horizon 的指标同时报两种聚合——horizon 均值（逐 horizon 算指标再平均）与扁平化整体（全部预测点 pooled）。主口径=扁平化整体；README 注明。注：每个 horizon 点数相等（balanced）时，MAE 两口径数学等价，差异只出现在 RMSE/sMAPE。
- 2026-09-10（P3 实现）：基线评估集=val+test origins；XGBoost 用 MultiOutputRegressor 直接 24 输出；DLinear 用 neuralforecast cross_validation（cutoff=origin 对齐，逐段执行，early-stop 的 val_size 取训练段尾部，不触评估段）；neuralforecast 3.2.2 的 DLinear scaler_type=identity（无全局缩放，符合硬约束 7 分层），显式 loss=MSE；PatchTST 默认值 patch_len=16/stride=8/revin=True 与论文 §3.1 一致，无冲突（13a 门禁预检通过）。
- 2026-09-10（P3 CV 泄漏修复）：rolling origin CV 原实现把最后一个 fold 的验证窗口设为 [cut_test, +∞)（即 test 段前 720 origins），违反硬约束 12。已改为 test 前 eligible origins 四等分、3 个内部切点滚动，全部验证窗口严格 < cut_test。作废受污染数字（fold3=0.4636），修正后重跑。教训：CV 边界条件用断言守护（va_idx 时间 < cut_test 已在新代码中由构造保证，后续 assert 复核）。
- 2026-09-10（P3 CV 最终数字）：folds 0.5941/0.5554/0.3867，mean 0.5121±0.0901（pooled MAE，XGBoost）。fold 间波动大是数据漂移的反映（2007 高负荷年 vs 2009 低负荷段），非实现 bug；P4 调参稳定性以此基线为参照。
- 2026-09-10（硬件配置，用户更新）：RTX 4060 Laptop（8GB 显存）+ R7 7435H；P4 训练用 GPU。torch 需 CUDA build（原装的 2.14.0+cpu 不满足，已换装 CUDA 版）。
- 2026-09-10（P4 GPU 训练规约，用户要求）：① 训练前必须验证 torch.cuda.is_available()，不可用先修 CUDA 环境，禁止静默回退 CPU 闷跑；② batch_size 上限 256，每 epoch 打印 max_memory_allocated；③ 预测张量及时移回 CPU 禁止 GPU 累积，每 epoch 结束 log 到 training_log.csv（含 epoch 耗时，用于核算 GPU 实际加速比）；④ 时间盒重谈：P4 预算 2 天 CPU → 6 小时 GPU，省下的时间优先保证误差归因与 README 质量；若提前完成且 rolling CV 稳定，向用户汇报是否解锁 OPT-1（用户决策，不自行解锁）。
- 2026-09-10（P3 DLinear 全量失败根因与重构）：no-refit cross_validation 在「评估跨度 >> 训练历史」时段失效——seg4/seg5/seg7 的 n_win 占段长 86-90%，模型只拿约 8 天历史训练再评估 1-5 个月，2010 年段 MAE 爆到 1.2-1.6。根因诊断：同段仅取末 1000 窗口（训练历史 2500h）时 no-refit MAE=0.334 正常。重构为统一滚动 168h 块协议（scripts/eval_rolling.py）：每块只用数据 <块起点 重训；跨段块按段拆开；对比表仅用所有模型共同覆盖 origins。test origins 分布：仅 seg5(2010-04-10 起)/seg6/seg7，seg6/seg7 训练历史先天 ≤8 天（数据限制，README 误差归因须说明）。XGBoost 同协议滚动重训（公平）。val(4,929) 用于 PatchTST 调参与早停；test(4,925) 只评一次。
- 2026-09-10（环境修复记录）：neuralforecast 3.2.2 中 pl.Trainer 额外参数须以 **kwargs 直连模型构造器（如 callbacks=[...]），传 trainer_kwargs={...} 字典会原样转发报 TypeError；CPU 基准须设 accelerator="cpu", devices=1；torch 2.6.0+cu124 + RTX 4060 Laptop 验证通过；EpochLogger 的 logged_metrics 键待修（train_loss 为 nan，应回退 train_loss_epoch/train_loss_step）。CPU 基准已录 training_log.csv（device=cpu，200 epoch，约 0.8-1.0s/epoch）。
- 2026-09-10（P4 滚动协议 bug 修复，重要）：`blocks_of` 原实现用 `times.view('int64')` 取纳秒常量整除分块，但 hourly.parquet 读回的索引是 datetime64[us]（微秒），整除全部归 0 → 每段合并成一块 → 退化为「每段单模型」，恰好踩中本协议要防的 stale 陷阱；且段首块训练史实测 167h<168 被跳过，seg3/seg4 整体无 DL 预测。已改为 Timedelta 整除（单位无关）。作废受影响结果：ptst_lr1e4 与 xgb_base 的 val npy（旧 xgb val MAE 0.4783 为单 fit 口径，无效），val/test 全部重跑。分块实测：val 31 块（2 块短史跳过，DL 覆盖 4,593/4,929）；test 31 块（2 跳过，覆盖 4,589/4,925）。每段首 7 天无 DL 预测是协议固有（段内历史 <168h 无法构造训练窗），README 误差归因须说明。
- 2026-09-10（P4 val 滚动最终结果，共同 origins n=4,593，pooled）：XGBoost 0.4727 (R²0.4484) ｜ PatchTST lr=5e-4 0.5788 (R²0.2303) ｜ PatchTST lr=1e-4 0.6011 ｜ DLinear 0.6087 (R²0.1864)。DL 稳定落后 XGB ~0.1 MAE，与论文「大数据集才显著」预期一致，进 P5 论文对照表。test 选型=XGB base / PatchTST lr=5e-4 / DLinear base（各模型 val 最优）。PatchTST val 全程 29 块×~155s≈75min/配置。test 评估按用户指令冻结，待 eval_rolling.py+窗口构造通过 API K3 泄漏专项审查后启动。
- 2026-09-10（early_stop_patience_steps=0 语义核实，用户要求）：查 neuralforecast 3.2.2 源码确认 0 = 禁用早停，非「首次验证即停」。依据：common/_base_model.py:167 默认 -1；:388 `if early_stop_patience_steps > 0:` 门控——仅 >0 才实例化 pytorch_lightning EarlyStopping(patience=...) 并 append 进 trainer callbacks（:397-402），0/-1 时回调不存在；core.py:729-731 仅当任一模型 patience>0 才强制 val_size>0，反证 0 时 fit(val_size=0) 合法。eval_rolling.py 短史块（val_size=0）传 0 的做法正确；patience 的「容忍 N 次不改进」语义只在 >0 时才有意义。
- 2026-09-10（泄漏审查通过与 test 启动）：用户贴 K3「逐项审计结果」——特征/目标构造、滚动协议（两道运行时断言获好评）、13a/13b 合规、分块修复全部通过；唯一低风险项 early_stop_patience_steps=0 已源码核实关闭（0=禁用早停）。test 三模型滚动评估正式启动：XGBoost(base)/PatchTST(lr=5e-4，val 最优)/DLinear(base)，按 val 选型只评一次；test 31 块（2 短史跳过，DL 覆盖 4,589/4,925）。跑完按 8 项清单汇报，用户确认后进 P5。
- 2026-09-10（test 汇报口径，用户确认）：按 8 项清单汇报——①协议与覆盖数 ②三模型+3 无状态基线对比表（pooled MAE/R²/RMSE/sMAPE，共同 origins）③test horizon-MAE 曲线 ④训练耗时/显存/时间盒余量 ⑤val→test 排序一致性核对 ⑥双路径指标核对（Pandas vs SQL）⑦已知限制（seg6/7 短训练史、每段首 7 天无 DL 覆盖）⑧数字留痕文件清单。用户确认后进 P5。
- 2026-09-10（断电安全停止，断点续跑备忘）：宿舍停电，test 评估在 PatchTST 第 1 块 epoch 284 处停止，XGB test 同步停止；tasklist 确认无残留 python 进程；npy 只在全程结束才落盘，outputs/p5 无半成品/损坏文件（仅存 4 个有效 val npy）。明早续跑顺序：①XGB test（CPU）+ ②PatchTST test（GPU lr=5e-4）并行 → ③DLinear test（GPU）→ 跑完按已确认 8 项清单汇报 → 用户确认后进 P5。泄漏审查已通过（逐项审计结果.md 存档），不需重审。
- 2026-09-11（test 三模型滚动完成，来电续跑）：共同 origins 4,589，pooled MAE：XGBoost 0.4190 (R²0.3377) ｜ ma7 0.4341 ｜ PatchTST lr5e-4 0.4894 (R²0.1451) ｜ DLinear 0.5011 ｜ seasonal 0.5105 ｜ persistence 0.6797。双路径核对全 OK。val→test 排序一致（XGB<PTST<DLinear 两期相同），无反转；三模型 test 均优于 val（2010 负荷水平低、绝对误差收窄）。horizon 曲线：XGB h1 0.319→h24 0.424(+33%)、PTST 0.407→0.494(+21%)、DLinear 基本持平；旧项目递归口径 h1 0.3943→h24 0.5761(+46%)——直接多输出曲线更平，归因材料入 P5。GPU 实测 median epoch 0.072s、峰值 730MB、加速比 ≈10.6×；P4 GPU 用时约 4h/6h 预算，余量 ~2h。注意：ma7_same_hour 无状态基线仅落后 XGB 0.015，P5 叙事须如实呈现「简单模型在小数据上的强竞争力」。
- 2026-09-11（P5 开工，OPT-1 有条件解锁 + B1 粒度桥接）：用户 P5 指令——消融仅 val 单次（patch 24/12，lr 不动，止损 9-12 18:00，结果不推翻 test 结论），已加 --patch-len/--stride 参数（默认 16/8 不变）并后台启动 run-id ptst_lr5e4_p2412。B1 粒度桥接：23:00 origins 预测=次日完整 24h 聚日均，PatchTST 日均 MAE 0.1866（R²0.3442，n=191 天）≈ 旧项目日级 XGB 0.1877（R²0.3347）——小时级模型在日粒度上与旧项目打平；XGBoost 日均 0.1503（R²0.5157，n=205）优于旧项目；DLinear 0.1712。注意口径差异：桥接用 test 段（2010-04~11）而旧项目为全数据日级，仅叙事桥接不作严格胜负。落盘 outputs/p5/granularity_bridge.csv。
- 2026-09-11（B1 修正 + B2 对比表）：B1 按主会话指令纯后处理修正——XGBoost 桥接行改用 DL 共同覆盖的 191 天（23:00 origins 交集）重算：日均 MAE 0.1504 / R²0.5268（原 205 天口径 0.1503 作废）；全表统一 n=191，footnote 注明叙事桥接不作严格胜负。教学案例记录：日粒度排序翻转（DLinear 0.1712 反超 PatchTST 0.1866），与 horizon 曲线互证（DLinear h1→h24 涨幅≈0% 本质是常数外推，日均聚合天然占优）——「聚合粒度改变模型排序」非错误。B2 新旧对比表落盘 outputs/p5/new_old_comparison.csv（11 行无缺行）：MAPE（旧 21.15%）与 sMAPE 分两列禁止混排；旧项目小时级单步 0.3238/R²0.5905 仅表后注明、不进表。XGBoost 特征 Top3（24 输出平均重要性，落盘 xgb_feature_importance.csv）：hour 0.196 / roll_mean_168h 0.098 / dow 0.072。
- 2026-09-11（B2 修正，用户审）：①朴素基线行结论方向改正——0.1798<0.1877 是朴素基线胜，定为「XGBoost 落后朴素基线约 0.008kW，日粒度下特征增益未覆盖复杂度成本，系本项目小时级改造的核心动机」；②XGBoost 特征 Top3 标注「代表性单配置重训，非全块平均」（仅新项目两行，旧项目行保持其自有 importance 口径）。已更新 new_old_comparison.csv。教训：基线对比行结论须先核大小方向再写定性语。
- 2026-09-11（B3 horizon 双曲线）：outputs/p5/horizon_mae_dual_curve.png——PatchTST/XGBoost/DLinear 全 24 点曲线（test n=4,589）+ 旧项目递归法首尾锚点（0.3943→0.5761, +46%）线性示意。结论：直接多输出显著压平误差累积（PTST +21%、DLinear ≈0% vs 递归 +46%）；XGBoost +33% 曲线前 3 小时陡升后持平，h1 优势（0.319）来自滞后特征对极近步的强预测力。图注已声明旧项目中间点未披露。
- 2026-09-11（README 终稿待办登记，用户指示）：B3 图标题口径细化（旧项目锚点「仅首尾披露、线性示意」的说明提升到标题/副标题层级）——终稿时改，现在不动图。
- 2026-09-11（B4 误差归因，预记，待随消融后正式汇报）：落盘 error_attribution.csv + error_by_period.png + error_by_hour.png。三维拆解（test 共同 origins n=4,589）：①时期——Q4（seg6/7 短训练史段）三模型 MAE 全面最高（XGB 0.505/PTST 0.616/DLinear 0.639），短训练史+漂移是 DL 落后的主因；Q3 夏谷绝对 MAE 最低（负荷水平低）。②时段——夜间绝对 MAE 最低（XGB 0.229）但 DL 夜间 nMAE 最高（PTST 0.657/DLinear 0.705，负荷近零值区域相对误差放大）；晚间 18-23 绝对误差最高（负荷峰值区）。③周内——周末 MAE 显著高于工作日（XGB 0.510 vs 0.384），周末行为模式方差大。④R² 分母效应——test 目标方差 0.503 仅为 val（0.789）的 64%，R² 跨期比较有分母效应，须配 nMAE/MAE 同读；Q4 方差 0.72 不低（均负荷 1.18），该段 R² 低是真实预测力问题非分母效应。
- 2026-09-11（OPT-1 消融结果，val 共同 origins n=4,593，pooled）：patch 24/12 vs 论文默认 16/8——MAE 0.5719 vs 0.5788（Δ -0.0069，+1.2%），R² 0.2488 vs 0.2303，RMSE 0.7686 vs 0.7780。24/12 略优但幅度 <1.3%，远小于 val↔test 波动（±12%），不构成选型依据；按 P5 指令不推翻 test 结论，作为「已知限制+未来方向」入 README。消融维度关闭（只此一次，未联动其他超参）。落盘 outputs/p5/ablation_patch_24_12_val.csv。GPU 消融单块 ~90s，全程 ~50min。
- 2026-09-11（B5 论文声称对照表）：落盘 outputs/p5/paper_claims_check.csv，7 条声称——C4 patch 长度（消融终稿措辞按用户定稿：「改善 0.0069 kW（1.2%），不改变模型间排序与量级差距，瓶颈在数据规模与漂移而非配置」）、C5 DLinear 表 3 现象（部分复现：小时级 PTST 略胜、日粒度 DLinear 反超、两者均被简单模型压制）、C6 RevIN（部分复现+边界：救漂移、救不了夜间近零值弱信号区，nMAE 证据 0.66-0.70 vs XGB 0.42）；C1/C2 未验证（范围外，归因时间盒与任务定义）；C3 部分/不适用（单变量 CI 无意义）；C7 自监督未复现（小数据+预算，原因写明）。
- 2026-09-11（B6 README 全稿完成，待用户验收）：README.md 14 节——概览/数据清洗/EDA/滚动协议/泄漏排查专节（6 案例含时间戳单位 bug）/scaler-RevIN 分层（含 C6 边界）/PatchTST 论文出处逐项对照/误差归因/新旧对比+粒度桥接+排序翻转教学案例/论文声称 7 条/OPT-1 登记/已知限制/复现步骤/留痕清单。遗留 5 项全部处理：①B3 图重生成（标题含 n=4,589 与锚点示意口径）；②消融 Δ 行定稿措辞；③C6 边界入 RevIN 节；④importance 标注入 §9.4；⑤排序翻转入 §9.3。环境版本按实测记录（pandas 2.3.3——装 CUDA 版 torch 时从 3.0.5 降级，时间戳 bug 与版本无关）。
- 2026-09-11（GitHub 推送准备第一步完成）：只读盘点落盘 github_staging_plan.md（198 行）——项目总 223.7MB（data 149.8MB / outputs 23.7MB / lightning_logs 聚合 28.8MB=859 文件，逐块训练自动产物）；>1MB 文件 10 个（原始 txt 126.8MB、minute_clean.parquet 22.4MB、training_log.csv 19.7MB、4 个 3.8MB ptst 日志、旧 ipynb 1.4MB、y_test_full.csv 1.2MB）；非 git 仓库、无根 .gitignore/requirements.txt。只生成了盘点文件，未整理/复制/推送、未执行 git 命令。已把全文发用户转 K3 审定推送清单，等第二步指令。
